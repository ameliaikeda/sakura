package processor

// error messages for wrapped errors.
const (
	errBodyReadFailed     = "sakura/processor: reading body failed"
	errImageProcessFailed = "sakura/processor: failed to process image"
	errImageSizeUnknown   = "sakura/processor: unable to determine image dimensions"
)
